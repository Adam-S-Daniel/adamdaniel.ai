# gitleaks configuration. Extends the default ruleset with allowlist
# entries for known test fixtures so CI doesn't flag them as leaks.
#
# Real secrets (cloud-provider keys, GitHub PATs, JWTs, generic
# high-entropy strings) still trigger findings; only the literal
# fixtures below are suppressed.
#
# When adding a new test that hardcodes a fake-looking token, add it
# here rather than disabling gitleaks — as a `regexes` entry.
#
# Suppress by `regexes` — NEVER by `paths` (cms-platform issue #260). A
# `paths` entry is not a finding filter: gitleaks skips the file ENTIRELY,
# before any rule runs, so a REAL credential pasted into it is never
# reported. This config used to exclude e2e/admin-reviews-(auth|stats).spec.js
# that way. Measured on the pinned 8.30.1 over this repo's full history, the
# entry suppressed nothing — a config carrying ONLY that path and no regexes
# reports exactly what the bare default ruleset reports (5 findings either
# way) — but both paths this entry covers DO appear in this repo's history
# (as do the platform's other two fixture paths), so it sat one `git mv`
# away from re-arming a whole-file, all-rules blind spot.
#
# Narrowing a `paths` entry does not work either: `regexTarget = "secret"`
# is ACCEPTED — it names the allowlist's DEFAULT target, so it narrows
# nothing — and gitleaks SILENTLY IGNORES unknown TOML keys, so a
# `condition = "AND"` is dropped without warning and the path alone
# re-suppresses the whole file. Delete, do not narrow.
#
# The "Allowlist canary" step in cms-platform's
# .github/workflows/secrets-scan.yml enforces this: it plants a detectable
# credential at every allowlisted path and fails if the config cannot see
# it. It only probes paths matching files TRACKED at scan time, though, so
# an entry matching nothing passes vacuously — so it would still pass on
# the day the entry re-armed against an untracked path. (It is not what let
# this one survive: the entry dates from 2026-04-29 and the canary only
# reached this repo with the v0.1.84 bump, so for its whole life there was
# no canary at all.)

[extend]
useDefault = true

[[allowlists]]
description = "adamdaniel.ai test fixtures"
regexes = [
  # Belt-and-braces: any ghp_*test*/ghp_*fake* literal anywhere.
  '''ghp_(test|fake)[A-Za-z0-9_]*''',
]

# NO skills.lock entry is needed here any more. The lock used to store a BARE
# 64-hex digest, and gitleaks' generic-api-key rule fired on the one line whose
# key carries a keyword ("cms-platform/cms-platform-secrets"), so an allowlist
# suppressed that line shape. The generator now writes `sha256:<hex>`
# (agentskills#87): `:` falls outside the rule's capture class, which truncates
# the capture below its 10-character floor, so the rule cannot fire at all and
# there is nothing left to allowlist. Do not re-add an entry for it — if a lock
# line ever trips this scan again, the lock has lost its label and THAT is the
# bug.
