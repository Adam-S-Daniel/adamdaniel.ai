---
# Custom site-wide Atom feed.
#
# Why this file exists instead of letting jekyll-feed emit /feed.xml:
# jekyll-feed has NO per-post exclude hook, and e2e / test-fixture posts
# live in the PUBLIC `posts` collection (see _plugins/exclude_e2e_posts.rb
# for the full root-cause writeup). jekyll-feed skips generating a feed at
# a path that already exists in the source tree (its generator does
# `next if file_exists?(path)`), so providing our own `feed.xml` here at
# the default `/feed.xml` path fully REPLACES the plugin's output — and
# lets us filter the e2e fixtures out via the shared `feed_exclude` marker
# the plugin stamps.
#
# The markup mirrors jekyll-feed's own feed.xml template (and the per-tag
# `_layouts/atom_feed.xml`) so any reader that parses one parses all three.
# `layout: null` keeps this from being wrapped in a site layout;
# `sitemap: false` keeps the feed itself out of the sitemap (jekyll-feed
# set the same on its generated page).
layout: null
sitemap: false
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom"{% if site.lang %} xml:lang="{{ site.lang }}"{% endif %}>
  <generator uri="https://jekyllrb.com/" version="{{ jekyll.version }}">Jekyll</generator>
  <link href="{{ page.url | absolute_url }}" rel="self" type="application/atom+xml" />
  <link href="{{ '/' | absolute_url }}" rel="alternate" type="text/html"{% if site.lang %} hreflang="{{ site.lang }}"{% endif %} />
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <id>{{ page.url | absolute_url | xml_escape }}</id>
  {% assign feed_title = site.title | default: site.name %}
  {% if feed_title %}
  <title type="html">{{ feed_title | smartify | xml_escape }}</title>
  {% endif %}
  {% if site.description %}
  <subtitle>{{ site.description | xml_escape }}</subtitle>
  {% endif %}
  {% if site.author %}
  <author>
    <name>{{ site.author.name | default: site.author | xml_escape }}</name>
    {% if site.author.email %}
    <email>{{ site.author.email | xml_escape }}</email>
    {% endif %}
    {% if site.author.uri %}
    <uri>{{ site.author.uri | xml_escape }}</uri>
    {% endif %}
  </author>
  {% endif %}
  {%- comment -%}
    Exclude e2e / test-fixture posts. `feed_exclude` is stamped by
    _plugins/exclude_e2e_posts.rb on any post whose slug starts with
    `e2e-` or that sets `test_fixture: true` — the single shared
    discriminator so this surface can't drift from the sitemap / listings.
    Jekyll already drops `published: false` posts from `site.posts`, so no
    extra published filter is needed here.
  {%- endcomment -%}
  {% assign posts = site.posts | where_exp: "post", "post.feed_exclude != true" | sort: "date" | reverse %}
  {% assign posts_limit = site.feed.posts_limit | default: 10 %}
  {% for post in posts limit: posts_limit %}
  <entry{% if post.lang %} xml:lang="{{ post.lang }}"{% endif %}>
    {% assign post_title = post.title | smartify | strip_html | normalize_whitespace | xml_escape %}
    <title type="html">{{ post_title }}</title>
    <link href="{{ post.url | absolute_url }}" rel="alternate" type="text/html" title="{{ post_title }}" />
    <published>{{ post.date | date_to_xmlschema }}</published>
    <updated>{{ post.last_modified_at | default: post.date | date_to_xmlschema }}</updated>
    <id>{{ post.id | absolute_url | xml_escape }}</id>
    {% assign excerpt_only = post.feed.excerpt_only | default: site.feed.excerpt_only %}
    {% unless excerpt_only %}
    <content type="html" xml:base="{{ post.url | absolute_url | xml_escape }}"><![CDATA[{{ post.content | strip }}]]></content>
    {% endunless %}
    {% assign post_author = post.author | default: post.authors[0] | default: site.author %}
    {% assign post_author = site.data.authors[post_author] | default: post_author %}
    {% assign post_author_email = post_author.email | default: nil %}
    {% assign post_author_uri = post_author.uri | default: nil %}
    {% assign post_author_name = post_author.name | default: post_author %}
    <author>
      <name>{{ post_author_name | default: "" | xml_escape }}</name>
      {% if post_author_email %}
      <email>{{ post_author_email | xml_escape }}</email>
      {% endif %}
      {% if post_author_uri %}
      <uri>{{ post_author_uri | xml_escape }}</uri>
      {% endif %}
    </author>
    {% if post.category %}
    <category term="{{ post.category | xml_escape }}" />
    {% elsif post.categories %}
    {% for category in post.categories %}
    <category term="{{ category | xml_escape }}" />
    {% endfor %}
    {% endif %}
    {% for tag in post.tags %}
    <category term="{{ tag | xml_escape }}" />
    {% endfor %}
    {% assign post_summary = post.description | default: post.excerpt %}
    {% if post_summary and post_summary != empty %}
    <summary type="html"><![CDATA[{{ post_summary | strip_html | normalize_whitespace }}]]></summary>
    {% endif %}
    {% assign post_image = post.image.path | default: post.image | default: post.featured_image %}
    {% if post_image and post_image != "" %}
    {% unless post_image contains "://" %}
    {% assign post_image = post_image | absolute_url %}
    {% endunless %}
    <media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="{{ post_image | xml_escape }}" />
    <media:content medium="image" url="{{ post_image | xml_escape }}" xmlns:media="http://search.yahoo.com/mrss/" />
    {% endif %}
  </entry>
  {% endfor %}
</feed>
