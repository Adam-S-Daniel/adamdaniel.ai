name: Secrets scan
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  schedule:
    - cron: '0 7 * * 0'   # weekly full-history sweep
  workflow_dispatch:
permissions:
  contents: read
  pull-requests: read
jobs:
  scan:
    uses: Adam-S-Daniel/cms-platform/.github/workflows/secrets-scan.yml@ccedd3c26a5f9c61e6da1cac2327f32f43c4d9f7
