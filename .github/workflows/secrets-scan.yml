name: Secrets scan
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  schedule:
    - cron: '0 7 * * 0'   # weekly full-history sweep
  workflow_dispatch:
permissions:
  contents: read
  pull-requests: read
jobs:
  scan:
    uses: Adam-S-Daniel/cms-platform/.github/workflows/secrets-scan.yml@19db64fb7277c26a7f83b7bcb7fa0db317edb60f
