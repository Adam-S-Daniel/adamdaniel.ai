name: CMS Editorial Workflow

# Triggered when Decap CMS opens or updates a PR for a draft entry.
# Validates the content, applies cms/draft on open, and on PRs whose
# base isn't main also applies cms/preview-only — flagging that the
# content edit lives on a feature branch's preview tree and is not
# meant to flow to production.
#
# No `branches:` filter — Decap creates `cms/...` PRs against whatever
# `backend.branch` is configured to. On adamdaniel.ai/admin/ that's
# main; on preview-pr<N>.adamdaniel.ai/admin/ that's the PR's head
# ref (set by scripts/patch-preview-config.sh).

on:
  pull_request:
    types: [opened, synchronize, labeled]
    paths:
      - '_posts/**'
      - '_projects/**'
      - '_tags/**'
      - 'pages/**'
      # Canary collection used by the publish-loop e2e tests. Without this,
      # validate-content doesn't run on canary edits → no required check
      # exists → auto-merge can't fire on feature-branch PRs (the PR #78
      # unstable-rollup failure mode that issue #79 calls out).
      - '_e2e/**'

permissions:
  contents: read
  pull-requests: write
  issues: write

concurrency:
  # Cancel previous validation runs when the CMS pushes another commit
  # to the same draft PR (Decap pushes per-keystroke during editing).
  # Labeled events also pile up on rapid label toggling — same group.
  group: cms-editorial-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  validate-content:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2 (2026-01-09)
        with:
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Setup Ruby
        uses: ruby/setup-ruby@0cb964fd540e0a24c900370abf38a33466142735  # v1.302.0 (2026-04-15)
        with:
          ruby-version: '3.2'
          bundler-cache: true

      - name: Validate front matter
        run: |
          # `nullglob` makes `_posts/*.md` expand to an empty list rather
          # than the literal pattern when there are no posts (otherwise
          # grep tries to read `_posts/*.md` as a filename and the loop
          # falsely flags it as a missing-title post).
          shopt -s nullglob
          echo "Checking post front matter..."
          ERRORS=0

          for f in _posts/*.md; do
            # Every post must have title, date, and published fields
            if ! grep -q "^title:" "$f"; then
              echo "ERROR: $f is missing 'title' field"
              ERRORS=$((ERRORS+1))
            fi
            if ! grep -q "^date:" "$f"; then
              echo "ERROR: $f is missing 'date' field"
              ERRORS=$((ERRORS+1))
            fi
          done

          if [ "$ERRORS" -gt 0 ]; then
            echo "Found $ERRORS validation error(s). Please fix before merging."
            exit 1
          fi
          echo "All content validated successfully."

      - name: Jekyll build check
        env:
          JEKYLL_ENV: test
        run: bundle exec jekyll build --quiet

      - name: Apply draft label on new PR
        if: github.event.action == 'opened'
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3  # v9.0.0 (2026-04-09)
        with:
          script: |
            // Ensure the 'cms/draft' label exists
            try {
              await github.rest.issues.createLabel({
                owner: context.repo.owner,
                repo:  context.repo.repo,
                name:  'cms/draft',
                color: '1a2a5e',
                description: 'Content draft — not ready for publish',
              });
            } catch (_) {}

            try {
              await github.rest.issues.createLabel({
                owner: context.repo.owner,
                repo:  context.repo.repo,
                name:  'cms/ready',
                color: '22c27a',
                description: 'Content approved and ready to publish',
              });
            } catch (_) {}

            var labels = ['cms/draft'];

            // Mark CMS PRs whose base isn't `main` as preview-only — they
            // exist to demonstrate / test the parent feature branch's
            // changes against real content, and the content edit itself
            // is not meant to flow to main when the feature merges.
            if (context.payload.pull_request.base.ref !== 'main') {
              try {
                await github.rest.issues.createLabel({
                  owner: context.repo.owner,
                  repo:  context.repo.repo,
                  name:  'cms/preview-only',
                  color: 'f5a623',
                  description: 'CMS edit made on a feature-branch preview — drop this content from the parent branch when it merges to main',
                });
              } catch (_) {}
              labels.push('cms/preview-only');
            }

            await github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo:  context.repo.repo,
              issue_number: context.payload.pull_request.number,
              labels: labels,
            });

  auto-merge-when-ready:
    # Fires when an editor promotes a draft to "Ready":
    #
    #   - `decap-cms/ready` is what Decap CMS itself applies when the
    #     editor changes the Status dropdown from Draft / In Review to
    #     Ready. This is the path 99% of edits take.
    #   - `cms/ready` is the legacy bare-name label this workflow
    #     applies on `opened` (so a manual label add still works) and
    #     was the only label this job listened for before the Decap
    #     namespacing landed. Keeping it as a synonym means a hand-
    #     applied `cms/ready` from `gh pr edit --add-label` still
    #     promotes the draft.
    #
    # If both fire (rare — Decap usually only sets its own), the second
    # `labeled` event re-runs this job; `enablePullRequestAutoMerge` is
    # idempotent so the duplicate is a no-op.
    if: >
      github.event.action == 'labeled' &&
      (github.event.label.name == 'cms/ready' ||
       github.event.label.name == 'decap-cms/ready')
    needs: validate-content
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Enable auto-merge for CMS PR
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3  # v9.0.0 (2026-04-09)
        with:
          script: |
            await github.graphql(`
              mutation($pullRequestId: ID!) {
                enablePullRequestAutoMerge(input: {
                  pullRequestId: $pullRequestId,
                  mergeMethod: SQUASH,
                  commitHeadline: "publish: ${context.payload.pull_request.title}"
                }) {
                  pullRequest {
                    autoMergeRequest { enabledAt }
                  }
                }
              }
            `, { pullRequestId: context.payload.pull_request.node_id });
