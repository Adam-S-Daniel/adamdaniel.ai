name: Platform bump
on:
  schedule: [{ cron: "0 7 * * 1" }]
  workflow_dispatch:
permissions:
  contents: write
  pull-requests: write
jobs:
  bump:
    uses: Adam-S-Daniel/cms-platform/.github/workflows/platform-bump.yml@d5cbc35117dbf6ba321ee6a06c2f90442a050c33
    secrets:
      gh_token: ${{ secrets.CMS_PLATFORM_PAT }}
