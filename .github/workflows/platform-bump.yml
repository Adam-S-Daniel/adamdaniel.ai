name: Platform bump
on:
  schedule: [{ cron: "0 7 * * 1" }]
  workflow_dispatch:
permissions:
  contents: write
  pull-requests: write
jobs:
  bump:
    uses: Adam-S-Daniel/cms-platform/.github/workflows/platform-bump.yml@94fce5e355f9db8aae68bbea4ebbda93193374ad
    secrets:
      gh_token: ${{ secrets.CMS_PLATFORM_PAT }}
