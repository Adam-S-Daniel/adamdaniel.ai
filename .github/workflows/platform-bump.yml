name: Platform bump
on:
  schedule: [{ cron: "0 7 * * 1" }]
  workflow_dispatch:
permissions:
  contents: write
  pull-requests: write
jobs:
  bump:
    uses: Adam-S-Daniel/cms-platform/.github/workflows/platform-bump.yml@19db64fb7277c26a7f83b7bcb7fa0db317edb60f
    secrets:
      gh_token: ${{ secrets.CMS_PLATFORM_PAT }}
