name: Platform bump
on:
  schedule: [{ cron: "0 7 * * 1" }]
  workflow_dispatch:
permissions:
  contents: write
  pull-requests: write
jobs:
  bump:
    uses: Adam-S-Daniel/cms-platform/.github/workflows/platform-bump.yml@bf307bc030cd3c6d93707ea377bc7e521fc57dc7
    secrets:
      gh_token: ${{ secrets.CMS_PLATFORM_PAT }}
