name: Platform bump
on:
  schedule:
    - cron: "0 7 * * 1"
  workflow_dispatch:
permissions:
  contents: write
  pull-requests: write
jobs:
  bump:
    uses: Adam-S-Daniel/cms-platform/.github/workflows/platform-bump.yml@ccedd3c26a5f9c61e6da1cac2327f32f43c4d9f7
    secrets:
      gh_token: ${{ secrets.CMS_PLATFORM_PAT }}
