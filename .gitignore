_site/
_site_demo/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
vendor/
*.gem
.bundle/
node_modules/
*.log
*.tmp
__pycache__/
*.pyc
.pytest_cache/
.aws-sam/
.claude/settings.local.json
.claude/worktrees/
test-results/
playwright-report/
screenshots/regression/
# Sandbox-only Playwright config that flips ignoreHTTPSErrors so the unpkg
# Decap bundle loads behind an egress TLS proxy (Claude-Code-web sessions).
# Never belongs in CI — see the admin-e2e gotchas in AGENTS.md.
playwright.localcert.config.js
bin/
e2e/visual-regression.spec.js-snapshots-before/
.claude/scheduled_tasks.lock
admin/commit.json
# Intermediate JSON records from e2e/manual-capture.js. The collator
# (scripts/build-contributor-manual.js) reads these and emits
# docs/CONTRIBUTOR_MANUAL.md, which IS committed. The raw JSON is
# regenerated on every capture run, so checking it in adds noise.
manual-capture/

