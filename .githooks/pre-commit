#!/usr/bin/env bash
# Pre-commit guard for the skills mirror.
# Used on Git < 2.54 via `git config --local core.hooksPath .githooks`,
# wired up by scripts/bootstrap.sh / bootstrap.ps1.
set -e
exec "$(git rev-parse --show-toplevel)/scripts/verify-skills-mirror.sh" --staged
